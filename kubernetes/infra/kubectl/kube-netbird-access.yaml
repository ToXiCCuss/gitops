# NetBird groups. The access policies between them are created by hand in the
# dashboard (the operator writes none, NetBird is deny-by-default) - see
# kubernetes/README.adoc, section 5.
#
#   k8s-admins          peers/users that may reach every app (policy -> kubernetes-services)
#   kubernetes-services every NetworkResource is in this group
#   k8s-<app>           destination group of one app's NetworkResource
#   <app>-users         peers/users that may reach only that app (policy -> k8s-<app>)
apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: kubernetes-services
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: kubernetes-services

---

# Formerly "kubernetes-clients". metadata.name stays, so the operator renames the
# existing NetBird group in place (same ID: peers, policies and the zone's
# distribution group keep working).
apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: kubernetes-clients
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-admins

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-vault
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-vault

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: vault-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: vault-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-argocd
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-argocd

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: argocd-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: argocd-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-grafana
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-grafana

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: grafana-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: grafana-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-harbor
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-harbor

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: harbor-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: harbor-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-jenkins
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-jenkins

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: jenkins-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: jenkins-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-backstage
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-backstage

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: backstage-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: backstage-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-akhq
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-akhq

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: akhq-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: akhq-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-apicurio
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-apicurio

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: apicurio-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: apicurio-users

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: k8s-databasus
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: k8s-databasus

---

apiVersion: netbird.io/v1alpha1
kind: Group
metadata:
  name: databasus-users
  namespace: netbird
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  name: databasus-users
