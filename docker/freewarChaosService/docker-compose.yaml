services:
  freewar-chaos-service:
    image: freewar-chaos-service:1.0.0
    container_name: freewar-chaos-service
    restart: unless-stopped
    ports:
      - "8081:8080"
    env_file:
      - "default.env"
      - ".env"
    extra_hosts:
      - "host.docker.internal:host-gateway"
