services:
  freewar-chaos-service:
    image: freewar-chaos-service:0.0.1-SNAPSHOT
    container_name: freewar-chaos-service
    restart: unless-stopped
    ports:
      - "8081:8080"
    env_file:
      - "default.env"
      - ".env"
    extra_hosts:
      - "host.docker.internal:host-gateway"
